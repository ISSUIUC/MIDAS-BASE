# Ground Station Data Ingestion

Something more will be written here later, for now:

Once you've installed uv (the Python package manager), just 
use `uv run ./src/gui_main.py`, and it should handle the rest.